'use client';

export function AuthStatus() {
  // O contexto de autenticação foi removido. Este componente pode ser removido ou adaptado para o novo fluxo.
  return <div>Autenticação: fluxo centralizado</div>;
} 